const test = [1, 2, 3, 4, 5];
const test2 = {
  a: true,
  b: false,
};

const fn = (a, b) => a + b;
const fn2 = (a, b) => a - b;

const car = { type: 'Ford', model: 'Focus', color: 'red' };
const car2 = { type: 'Opel', model: 'Astra', color: 'black' };
